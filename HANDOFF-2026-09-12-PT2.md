# 断点交接 (第二批) - 2026-09-12 05:5x

迁移目标: 另一个 agent 接手继续开发. 定位如下:

- **本文档自包含**, 覆盖"接下来要做什么". 不需要任何聊天上下文.
- **历史与已验证数据**请看 `HANDOFF-2026-09-12.md` 与 `DEVLOG.md` Session 44.
  本文档**取代** `HANDOFF-2026-09-12.md` 的第 8 节(建议的接手顺序)及其待办状态,
  其余章节(仓库状态/跨项目侦察/反编译调研摘要)仍然有效.

---

## 0. 一句话状态

代码**零改动**, HEAD = `26b120c`, 工作区只有未跟踪的 `research/`.
本轮做的是: 把第 1 步(改名 + 三处文本修复 + id 迁移)的**全部技术前提调研完并实测验证**,
并把原版东尼算法的反编译研究做完. **实现一行没写**.

---

## 1. 用户已裁定的方向 (决断, 不要再问)

| # | 决断 | 备注 |
|---|---|---|
| D1 | 中文显示名 `东尼算法 - 遗物` -> **`怪异炼化 - 遗物`** | |
| D2 | 英文显示名 -> **`Qurious Crafting - Relics`** | |
| D3 | **mod id 要改**: `AutoAnthonyRelics` -> **`QuriousCraftingRelics`** | **推翻** `HANDOFF-2026-09-12.md` 0.1 的"只改显示名, 不要改 id" |
| D4 | id 迁移**现在就做**, 与改名/文本修复一起, 只构建部署一轮 | |
| D5 | 范围 = `HANDOFF-2026-09-12.md` 第 8 节 8 步按顺序全做 | |
| D6 | 过程写 `DEVLOG.md`; 聊天只报方向/决断/问题 | |
| D7 | 未经许可不发布 Steam | 沿用 |

D3 的理由(用户原话要点): 研究完东尼算法机制后要做**真正复原特性**的东尼算法遗物,
旧 id `AutoAnthonyRelics` 腾给它. 所以**不要再为了"保 cfg / 保联机"而试图保住旧 id**.

---

## 2. 本轮已完成并验证 (可直接用, 不必重做)

### 2.1 (A) 配置文本显示为变量名 - 根因与修法已实测闭环

**根因**: BaseLib `ModConfig.GetLabelText` (`research/BaseLib-StS2/Config/ModConfig.cs:499-503`)
用 `StringHelper.Slugify(名字)` 拼 `{ModPrefix}{slug}.title`; 查不到就**回退显示原始名字**.
`Slugify` = 正则 `([A-Za-z0-9]|\G(?!^))([A-Z])` -> `$1_$2`, 再 `\s+`->`_`, 再删 `[^A-Z0-9_]`, 全大写.

**实测** (真 .NET 正则, 非模拟): 152 个候选名里 **145 个失效**, 只有 7 个驼峰名正常.
证据 `research/tools/slug-ground-truth.txt`. 例:

```
Cost_C_START_DAMAGE_ALL -> COST_C_S_TA_R_T_D_AM_AG_E_A_L_L   (坏)
BUDGET_SECTION          -> B_UD_GE_T_S_EC_TI_O_N              (坏)
Cost_C_Start_Damage_All -> COST_C_START_DAMAGE_ALL            (好, 不动点)
Budget_Section          -> BUDGET_SECTION                     (好, 不动点)
```

**修法 (已验证, 采用这个)**: 名字按 `_` 切段, 每段首字母大写, 其余小写.
改名后 Slugify 成为不动点, 且**恰好等于现有的 loc 键** => **两个 loc 文件零改动**.

- 完整对照表: **`research/config-slug-map.tsv`** (152 行, 4 列: 现名 / 现 slug / 建议新名 / 新 slug).
  实现时按表逐项改, 不要手算.
- 重新生成该表: `research/tools/slug-map.ps1`.
- 核对新名是否命中现有 loc 键: `research/tools/verify-loc-keys.py`.
- 已核对结论: 47 个 `Cost_*`/`Refund_*` + 4 个区块名 = **51/51 命中现有 loc 键**.
  94 个 `Min_*`/`Max_*` 本来就没有 loc 键, 且带 `[ConfigHideInUI]` 不渲染, **无需 loc**.
- 区块名同理: `[ConfigSection("BUDGET_SECTION")]` -> `"Budget_Section"`, loc 不变.

**孤儿键 (顺手可删)**: `AUTOANTHONYRELICS-RESTORE_DEFAULTS_BUTTON.title` 是死键.
BaseLib 的"恢复默认"按钮走 `GetBaseLibLabelText` -> `BASELIB-RestoreDefaultsButton`, 不查我们的前缀.

### 2.2 BaseLib **没有**任何配置迁移钩子 (新发现)

grep 过 `Config/ModConfig.cs` 与 `Config/SimpleModConfig.cs`: 无 `Migrate`, 无 `OnLoad`, 无版本号.
唯一相关是 `RestoreDefaultsNoConfirm` (`ModConfig.cs:179`, virtual).
=> 改属性名 = 旧 cfg 键失效回退默认值. **必须自己写迁移.**

### 2.3 配置加载时机 (决定迁移代码放哪, 已确定)

```
ModConfig 构造函数 -> CheckConfigProperties(); Init();
Init() (ModConfig.cs:193-203):  if (File.Exists(_path)) Load(); else Save();
```

触发点: `MainFile.Initialize()` **第 28 行** `new AutoAnthonyRelicsConfig()`, **同步执行**.
=> 迁移必须放在第 28 行**之前**. 顺序是确定的, 不依赖任何巧合.
`_path = Path.Combine(OS.GetUserDataDir(), "mod_configs", filename)`,
`filename = 根命名空间(去特殊字符) + ".cfg"`.

### 2.4 ModPrefix 与 cfg 文件名来自**根命名空间**, 不是 mod id

`BaseLib.Extensions.TypePrefix.cs`:
`GetPrefix()` = 命名空间首段大写 + `"-"`; `GetRootNamespace()` = 命名空间首段.
=> **改命名空间**会同时改变 loc 键前缀和 cfg 文件名; 只改 mod id 不会.
(所以 id 迁移要么连命名空间一起改, 要么接受 loc 前缀仍叫 AUTOANTHONYRELICS-. 用户选了前者.)

### 2.5 cfg 里不能丢的用户调价 (迁移必须保住)

源文件 `C:/Users/o_Obl/AppData/Roaming/SlayTheSpire2/mod_configs/AutoAnthonyRelics.cfg`:

| 键 | 现值 | 默认 | 说明 |
|---|---|---|---|
| `ChaosRelicBudgetRare` | 30 | 24 | 稀有预算 |
| `Cost_C_START_ARTIFACT` | 5 | 9 | 人工制品单价 |
| `ChaosRelicNegativeChanceCommon` | 10 | 35 | |
| `ChaosRelicNegativeChanceUncommon` | 30 | 55 | |
| `ChaosRelicMultiplier` | 0 | 3 | 旧键, 已闲置 |

### 2.6 (B)(C) 修复点已定位

- `BudgetEditorPanel.EffectText` (`mod/Code/Patches/BudgetEditorPanel.cs:196-197`)
  调 `ChaosRelicGenerator.RenderOperation(spec, spec.Max)` => 显示**具体数字**.
- `RenderOperation` (`mod/Code/Chaos/ChaosRelicGenerator.cs:231-234`) 对懒惰模板用 `{M}` -> `7-amount`.
- **期望**: 编辑器行显示字面 `N`; 懒惰显示 `(7-N)`. 生成出的遗物**实际描述仍显示具体数字**.
- `RenderOperation` 的三处生成器调用 (`:148`, `:187`, `:217`) 必须保持原语义 => **要拆成两个方法**, 不要改 `RenderOperation` 本身.

### 2.7 原版东尼算法反编译研究 (已完成)

**`research/original-autoauthony-contract.md` (647 行, 已过文本检查器)**. 关键点:

- 原版不是"就地改牌", 而是把 481 张卡**离线拆成 931 个原子片段**(内嵌 JSON), 按 `角色+种子` 重采样拼出新卡池.
- **条件与效果在数据层就是独立片段**: 条件 = `RuntimeTriggerSpec`, 效果 = `OperationRuntimeSpec.Opcode`;
  各抽各的, 由 `LinkedTriggerIndex` 事后绑定, 普通触发还有 **50% 概率**(`_random.Next(2)`)不绑定
  => 这就是"打乱条件".
- 用户举的例子**已确证可达**: `A:whenOwnerHpLostDuringTurn` 原版既配 `N:Self`(撕裂, +力量)
  又配 `N:AllD`(狱火, 全体伤害); `N:HP-` 是池内独立效果原子, 接上去即"每失去生命, 失去 1 点生命".
- 随机源 `System.Random(SHA256("AutoAnthony/v111/all-pools-v2/{角色}/{seed}"))`, **可复现**;
  战斗内随机走引擎 `RunState.Rng`.
- **诅咒不在随机池** (`GeneratedCardType` 只有 Attack/Skill/Power); `Guilty` 的 `CombatsSeen`
  在原版 IL 里出现 **0 次**, 无补丁 => 这正是 0.3 要补的额外池.
- 完整 Harmony 补丁清单(128 个补丁类)与引擎 API 清单在文档里.

---

## 3. 待办: 8 步 (按顺序)

### 第 1 步 - 改名 + 三处文本修复 + id 迁移 (调研已完, 待实现)

#### 1a. id 迁移 `AutoAnthonyRelics` -> `QuriousCraftingRelics`

**引擎侧约束 (已查清)**:
- `ModManager.ReadModsInDirRecursive`: 递归扫 `mods/` 找 `*.json` 清单, **目录名不必等于 id**.
- `TryLoadMod`: `Path.Combine(mod.path, modId + ".dll")` (`ModManager.cs:796`) 与
  `... + ".pck"` (`:818`) => **dll / pck 文件名必须等于 manifest id**.

**必须改的清单** (当前 `AutoAnthonyRelics` 出现次数已统计):

| 文件 / 位置 | 改什么 |
|---|---|
| `mod/AutoAnthonyRelics.json` | 文件名 -> `QuriousCraftingRelics.json`; `id`; `name` |
| `mod/AutoAnthonyRelics.csproj` | 文件名 -> `QuriousCraftingRelics.csproj`(程序集名默认来自它); 内部 `<ModId>` 由它驱动 `$(ModId).json` / `$(ModId)/localization/**` / `$(ModId)/**` 编译排除 / pck 内容 / 拷贝目标 `$(ModsPath)$(ModId)/` |
| `mod/AutoAnthonyRelics/` | 目录 -> `mod/QuriousCraftingRelics/` (内含 `localization/{eng,zhs}/`) |
| `mod/project.godot` | `config/name`; `config/icon` 的 `res://` 路径; `[dotnet] project/assembly_name` |
| `mod/Code/MainFile.cs:17,18` | `ModId` 常量; `ResPath = $"res://{ModId}"` (res:// 是唯一引用点, 已集中) |
| 全部 C# 命名空间 | 根 `AutoAnthonyRelics` + `.Chaos` / `.Patches` / `.Models` / `.Pools` / `.Compat` / `.Extensions` / `.Code.DevConsole` |
| 两个 loc 文件 | 键前缀 `AUTOANTHONYRELICS-` -> `QURIOUSCRAFTINGRELICS-` |
| `standalone/RrcA4hKeyFix/StandaloneMain.cs` | 它声明 `namespace AutoAnthonyRelics;` 作为**垫片**, 给共享源 `RrcTreasureKeyCompat.cs` 提供 `MainFile`. **必须同步改**, 否则独立包编译失败 |
| `standalone/RrcA4hKeyFix/RrcA4hKeyFix.csproj` | 注释里引用了共享源与 `AutoAnthonyRelics` |
| `workshop/content/AutoAnthonyRelics/` | 目录 + 内部 dll/json/pck/pdb 全部改名 |
| `workshop/workshop_upload.vdf` | `title` / `description` / `contentfolder` |
| `workshop/*.txt`, `workshop/DESCRIPTION*.md` | 正文里的 `东尼算法 - 遗物` / `AutoAnthony - Relics` |
| `DEVELOP.md`, `DEVLOG.md` | 历史条目**不要改**(那是史实), 只在新增条目里记录本次迁移 |

**三个陷阱**:
1. `RelicsSettingsScreenPatch.cs:39` 的 `AutoAnthonySettingsGroup` 是**另一个 mod**(原版 AutoAnthony 卡牌 mod)
   的节点名, 用来做锚点定位. **全局替换时必须只替换 `AutoAnthonyRelics`, 不要替换 `AutoAnthony`**,
   否则会打断锚点. (`AutoAnthonyRelicsSettingsGroup` / `...GroupButton` / `...SettingsScroll` / `...SettingsOptions`
   是我们自己的节点名, 可以改也可以不改, 与功能无关.)
2. 命名空间一改, **loc 键前缀和 cfg 文件名同时变**(见 2.4), 所以 1a 与 1b 的 cfg 迁移必须一起做.
3. `standalone/RrcA4hKeyFix` 是孪生包, 靠 `Harmony.GetPatchInfo` 按声明类型 FullName 去重.
   改命名空间后要去重键同步变化, 别让两份都以为自己没装.

#### 1b. (A) 配置属性改名 + cfg 迁移

- 按 `research/config-slug-map.tsv` 逐项改名. 规则: 按 `_` 切段, 每段首字母大写其余小写.
- **同步改** `AutoAnthonyRelicsConfig.cs` 里的键构造:
  `LookUpCost` / `LookUpRefund` / `SetBoundProp` / `ApplyUserBounds` 拼的是
  `"Cost_" + template` / `"Refund_" + template` / `"Min_" + template` / `"Max_" + template`.
  template 是 `C_START_DAMAGE_ALL` 这类大写下划线形式, 需要加一个
  `TitleSnake(template)` 转换(就是同一条规则), 拼成 `Cost_C_Start_Damage_All`.
- **写一次性 cfg 迁移**, 放在 `MainFile.Initialize()` 第 28 行**之前**:
  1. 读旧 cfg (`AutoAnthonyRelics.cfg`), 逐键按同一规则改名;
  2. 写到**新**文件名 `QuriousCraftingRelics.cfg`;
  3. 旧文件改名备份而不是删除(例如 `<名>.cfg.v0.5.1.bak`);
  4. 整个迁移包 try/catch, 失败也不能中断 mod 初始化;
  5. 幂等: 迁移后不再有未迁移的下划线键, 不会重复触发.
- 验收: 用户调过的价(见 2.5)在新 cfg 里原样保留; 新 cfg 里没有 `Cost_C_START_*` 形态的键.

#### 1c. (B)(C) 编辑器文本

- 新增编辑器专用渲染(例如 `RenderEditorText(spec)`), 与 `RenderOperation` 并列放在
  `ChaosRelicGenerator.cs`: 普通模板 `{N}` -> `N`; 懒惰模板 `{M}` -> `(7-N)`.
- `BudgetEditorPanel.EffectText` 改调新方法. `RenderOperation` 保持原样.
- 顺手更新两个 loc 文件的 `BUDGET_HINT`, 说明行内的 `N` 就是下方滑条取的值.

### 第 2 步 - 构建 + 探针 + 部署三处 + 实机冒烟
见第 5 节命令. 构建必须 `0 warning / 0 error` 且打印 `PCK packed`.

### 第 3 步 - 原版机制契约
**已完成** (`research/original-autoauthony-contract.md`). 直接进入第 4 步.

### 第 4 步 - 按契约实现"真正的东尼算法-遗物"新 mod (含悔恨式额外池)
未开始. 契约文档已就绪. 额外池的引擎参考: `MegaCrit.Sts2.Core.Models.Cards/Guilty.cs`
(`[SavedProperty] CombatsSeen`, `>= 5` 且在牌组里时 `CardPileCmd.RemoveFromDeck`).

### 第 5 步 - 回填 F05 `VanillaRelicMapping.cs`
**数据集已全部验证**, 在 `HANDOFF-2026-09-12.md` 7.1, 直接照抄重写即可, 无需重新调研.
要点: `OurPointsFor` 必须走 `ChaosTemplates` 并用 **live config** 定价(`ChaosPointCosts`),
不要用 catalog 默认值; 遵守 `Decaying` 三角计价.

### 第 6 步 - 消除字典枚举顺序依赖 (MP 风险)
`ChaosRelicCatalog.Specs` 是 `Dictionary<string, TemplateSpec>(StringComparer.Ordinal)`,
`PositiveTemplates`/`NegativeTemplates` 由 `Specs.Values.Where(..)` 派生 => 顺序即字典枚举顺序.
推荐**按 template id 排序**从根上消除, 顺带让 `ConfigFingerprint` 稳定.
(未确证是缺陷, 但"实践上稳定"不是契约.)

### 第 7 步 - 7.2 的遗留
- `X_RETAIN_ATTACK_BUFF` 文案("本回合你的下一张攻击牌")与实现时机(`AfterFlush` 发放, 实际作用于**下一回合**)不符: 改文案或改实现.
- `OurPointsFor` 的 live 定价(与第 5 步重叠).
- 复核 `PickAffordablePositive` 的 `UniqueOnly` 是否真生效(当前代码看是硬排除了, 需实测确认).

### 第 8 步 - MpConfigSync 三缺陷 + 重派四个零产出审查
三个缺陷与四个任务书都在 `HANDOFF-2026-09-12.md` 3.3 / 7.4, 直接照做.

---

## 4. 关键决策与约定

- **保架构**: Config -> Catalog/PointCosts -> Generator(60 槽) -> RunRegistry -> ChaosRelicModel/LocUpdater -> 池替换 + 引擎钩子. 不重写.
- **先修契约再调价**: F05 回填前不要基于 `VanillaRelicMapping` 做调价.
- **用探针而非假设**: 所有结论要有隔离探针支撑.
- **"编译并部署" != "已实现"**: 未经实机/探针验证的功能一律视为未验证.
- **实机失败先隔离再归咎本 mod**: 宝箱房失败已 A/B 证明属 `RelicRewardChoices`.
- **孪生包去重是跨程序集的**: 用 `Harmony.GetPatchInfo` 按声明类型 FullName 匹配.
- **过程写 DEVLOG, 聊天只报方向/决断/问题** (D6).

---

## 5. 环境与命令

### 构建 (游戏需关闭, 或加 no-copy)
```bash
cd "G:/omp works/AutoAnthonyRelics/mod"
export DOTNET_CLI_HOME="G:/omp works/.dotnet-home" \
       NUGET_PACKAGES="G:/omp works/sts2-spire1/.nuget/packages" \
       NUGET_HTTP_CACHE_PATH="G:/omp works/sts2-spire1/.nuget/http-cache" \
       TEMP="G:/omp works/.tmp" TMP="G:/omp works/.tmp"
dotnet build AutoAnthonyRelics.csproj                                   # 必须打印 "PCK packed"
dotnet build AutoAnthonyRelics.csproj -p:CopyToModsFolderOnBuild=false  # 游戏占用 dll 时

cd "G:/omp works/AutoAnthonyRelics/standalone/RrcA4hKeyFix"
dotnet build RrcA4hKeyFix.csproj --no-restore -p:CopyToModsFolderOnBuild=false
```
注意: id 迁移后 csproj 文件名变了, 上面命令的 csproj 名要跟着改.

### 探针
```bash
cd "G:/omp works/.tmp/aar-assessment-20260912"
dotnet run --project Probe.csproj --no-restore
```
(基线 `assessment-2026-09-12-results.txt`, 修复后 `probe-2026-09-12-after-fix.txt`)

### 部署目标 (三处必须同步)
- `g:/steam/steamapps/common/Slay the Spire 2/mods/AutoAnthonyRelics/` -> 改名后为 `.../mods/QuriousCraftingRelics/`
- `g:/steam/steamapps/common/Slay the Spire 2/mods_disabled/AutoAnthonyRelics/` -> 同上
- `G:/omp works/AutoAnthonyRelics/workshop/content/AutoAnthonyRelics/` -> 同上

孪生包: `g:/steam/steamapps/common/Slay the Spire 2/mods/RrcA4hKeyFix/`

> 部署时要**删掉旧的 `AutoAnthonyRelics` 目录**, 否则旧 id 的 mod 还在, 会与新 id 的 mod
> 同时加载(两份混沌遗物池替换), 也可能触发 `DUPLICATE_ID` 之外的诡异行为.

### 其他
- 游戏: `g:/steam/steamapps/common/Slay the Spire 2/SlayTheSpire2.exe`
- 日志: `C:/Users/o_Obl/AppData/Roaming/SlayTheSpire2/logs/godot.log`
- 配置: `C:/Users/o_Obl/AppData/Roaming/SlayTheSpire2/mod_configs/<根命名空间>.cfg`
- autoslay: `--autoslay --seed <S> --log-file <path>`
- 反编译: `C:/Users/o_Obl/.dotnet/tools/ilspycmd.exe` (9.1)
- 引擎反编译源: `G:/omp works/sts2-spire1/research/engine-dllsrc/`, 索引 `research/engine-api-index.md`
- BaseLib 源: `G:/omp works/sts2-spire1/research/BaseLib-StS2/`
- 原版 AutoAnthony: `g:/steam/steamapps/workshop/content/2868840/3786611028/`

### 机器约定
- **绝不写 C:** 缓存/构建/临时全部导到 `G:`.
- Git: `user.name "Twelve-eight"`, `user.email "Twelve-eight@users.noreply.github.com"`.
  代理不通时用 `git -c http.proxy= -c https.proxy= push origin master`.
- 不要动 `.tmp/triple2-push.ps1` (共享).
- 未经明确授权, 不要传送/停止/重启正在运行的客户端.
- 文本语言卫生: 只允许中/英/法/德/俄 + ASCII. 检查器
  `node "G:/omp works/sts2-spire1/tools/check-agent-text.mjs" --file <path>`.
- 子代理只写代码, **跳过 build/lint/test**; 主会话集中构建. 每个写代码的子代理要在**同一批次**
  配一个 reviewer, reviewer 必须等实现者报告后再审.
- `.omp/hooks/pre/backup.ts` 在本仓库**已存在**.

---

## 6. 工具陷阱

- **PowerShell 工具不回传 stdout** (本环境实测: `Write-Output` 完全看不到, 连 `exit code` 都不可靠).
  要拿结果就**写文件再用 Read 读**. 另外整段脚本用 `& script.ps1` 跑经常静默失败;
  建议只把"需要真实 .NET 语义的那一小段"放进 PS 并写文件, 其余逻辑用 Python/Bash.
- **Python 的 `re` 不支持 `\G`**, 不要试图用 Python 复现 `Slugify`; 用 PS + 真 .NET 正则, 或直接用现成的 `research/config-slug-map.tsv`.
- 用 heredoc Python 读本仓库的 `.txt` (CRLF) 会**静默截断**. 改用 bash 层 `grep`/`sed`/`awk`,
  或显式 `io.open(..., encoding='utf-8', errors='replace')`, 或 `encoding='utf-8-sig'` 处理 BOM.
- `grep` 工具对 CRLF 行可能匹配不到. 分歧报告里字段名是 `relic-bag`.
- 从会话 jsonl 抽长字符串会静默截断, 切成 <800 字符的块.
- PowerShell 写 `.java`/文本会加 BOM; 用 Write 工具或 `-Encoding ASCII`.

---

## 7. 已知噪音 (不要重复上报)

- `Asset not cached: res://images/packed/card_template/card_frame_sdf.exr`
- `Volume can't be set to NaN.`
- 退出时的 RID/shader 泄漏警告
- `[Perfect] Harmony patch failed (AFTP absent?)`
- `[AutoAnthony] Expected 65 complete v111 Colorless cards, found 77`
- 宝箱房 `Proceed button not enabled after picking relics` (属 `RelicRewardChoices`, 已 A/B 证实)

---

## 8. 本轮新增的落盘产物

| 路径 | 内容 |
|---|---|
| `research/original-autoauthony-contract.md` | 原版东尼算法机制契约 (647 行) |
| `research/config-slug-map.tsv` | 152 行 x 4 列: 现名 / 现 slug / 建议新名 / 新 slug |
| `research/tools/slug-map.ps1` | 重新生成上表 (真 .NET 正则) |
| `research/tools/verify-loc-keys.py` | 核对新名是否命中现有 loc 键 |
| `research/tools/slug-ground-truth.txt` | 真 .NET 正则实测样本 |
| `DEVLOG.md` Session 44 | 本轮全部过程与已验证结论 |
