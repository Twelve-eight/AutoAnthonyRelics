# HANDOFF -- AutoAnthonyRelics 汉化对齐 + 点数预算编辑器 (2026-09-11)

> 交接文档 (会话被污染, 用户将开新会话). 新会话从此文档 + git 状态 + DEVLOG 恢复.
> Git: 所有改动已 commit+push, 工作区干净 (commit 56ff101). 构建 0 warn 0 err, 已部署三处.

## 会话背景与授权
- 用户已关游戏并授权部署测试. 游戏当前**未运行** (mods/ 可自由部署).
- 本轮目标: (1) 全部汉化文本对齐游戏原版 zhs 术语; (2) 按药水/卡牌价值基准重审词条点数; (3) 升级点数分配编辑窗口 (原版遗物引用 + 悬停详情 + Min/Max 双端滑条 + 每点计价).
- 会话后半段被 gpt5.6sol 敏感词污染 -> 落盘交接, 开新会话.

## 已完成 (commit 56ff101, 全部已推送)

### A. 术语对齐 (数据来源: 运行时 loc dump + pck 静态提取, 全部字节级核实)
- **人工制品** = ArtifactPower (原"护体"). 原版 ARTIFACT_POWER.title = 人工制品, description = "免疫负面效果". 怪物可持有 (BronzeAutomaton 3层/SphericGuardian 3层/Sentry 1层).
- 覆甲 = PlatingPower (原"镀层", 上轮已改, 本轮再验: 原版 title=覆甲, "回合结束时获得格挡, 回合开始减少1层").
- 懒惰 = SLOTH_POWER.title (原"怠惰"). 原版 description: "你在每个回合不能打出超过3张牌".
- 力量/敏捷/脆弱/虚弱/易伤/中毒/再生/荆棘/缓冲 = 原版同名.
- 关键词: 虚无=ETHEREAL, 保留=RETAIN, 奇巧=SLY ("如果这张牌在你的回合结束前从你的手牌中被丢弃,则免费将其打出"), 消耗=EXHAUST, 固有=INNATE.
- 附魔: 锋利=SHARP (伤害+N), 灵巧=NIMBLE (格挡+N, 仅能附魔"获得格挡"的牌), 注能=IMBUED (战斗开始时自动打出, 仅技能牌). 旧文案"锋锐(伤害+2)/轻盈(费用-1)/灌注(升级该牌)"**机制描述错误**, 已修正.
- 姿态 (Watcher mod): 愤怒=双倍伤害双向 (旧"+50%"), 平静=**离开时**获得2能量 (旧"回合结束时"), 神格=三倍伤害+进入时3能量+下回合自动退出 (旧"翻倍+回合结束3能量").
- 覆甲词条描述已简化去掉机制附注 (悬停原版遗物可看详情 - 见 E).

### B. 重定价 (用户裁定: 人工制品显著贵于力量/敏捷)
- 药水基准 (反编译核实): 力量药水=2力量, 敏捷药水=2敏捷 (都是 Common; 用户记忆"敏捷3"是 STS1). 无原版人工制品药水.
- 核心电涌 (Spire1 mod 卡): 11伤害 + 1人工制品 -> 人工制品单层价值极高.
- **StartArtifact: CostPerPoint 5 -> 9**, Min/Max 1,1 -> 1,2 (catalog + config `Cost_C_START_ARTIFACT` 已同步 = 9).
- 其余词条点数未动 (本会话没来得及全面复审, 见 Pending).

### C. locdump 控制台命令 (开发工具, 保留)
- `mod/Code/DevConsole/LocDumpConsoleCmd.cs`: `locdump [table] [lang]` -> 写 JSON 到 `G:/omp works/.tmp/locdump/<lang>/`. 已实测: 5 表 6736 键成功 dump (powers/relics/gameplay_ui/cards/potions zhs).
- 控制台开关键 = 键盘 `` ` `` (Quoteleft). 命令自动注册 (ReflectionHelper.GetSubtypesInMods).
- 实测流程: 启动游戏 -> 发按键 (PowerShell SendKeys, 见 `.tmp/send-locdump.ps1`) -> 读日志 `[INFO] [AutoAnthonyRelics] [locdump] ...`.

### D. pck 静态提取 (重大发现, 工具在 .tmp 不进 git)
- `G:/omp works/.tmp/pck-extract/pckv3.py`: Godot 4.5 pck v3 **目录在文件尾部** (非加密! flags=2 = REL_FILEBASE 而非 ENCRYPTED). 头部: magic+ver+4.5.1+flags+file_base(112)+dir_end(0x20), 数据从 0x70 起 32 对齐, dir_end 处 count u32 + entries (name_len(pad4)+name+offset(i64, REL)+size+md5+flags).
- 已用它对主 pck (15890 文件) 提取全部 zhs loc: `G:/omp works/.tmp/pck-extract/game-loc/localization/zhs/*.json` (49 表).
- 另提取了 ActsFromThePast.pck, Watcher.pck, Spire1.dll 反编译 (`G:/omp works/.tmp/spire1-asm/`, `afp-asm/`, `watcher-loc/`).
- 术语基准文件: `G:/omp works/.tmp/locdump/zhs-baseline.txt` (330 行), 原版遗物库 `G:/omp works/.tmp/locdump/vanilla-relics-zhs.json` (306 遗物).

### E. VanillaRelicMapping (新文件, 已提交)
- `mod/Code/Chaos/VanillaRelicMapping.cs`: 词条 -> 原版遗物引用数组 (RelicId, DisplayName zhs, Description 全文, Rarity, EffectNote 数值).
- 已映射: StartBlock->锚(10格挡/Common), StartStrength->金刚杵(1/Common)+玉之剑(3/Event)+余烬茶(2/Event), StartDexterity->意外光滑的石头(1/Common)+皮带扣(2/Common), StartDraw->准备背包(2/Common)+蛇之戒(2/Common), StartThorns->铜质鳞片(3/Common), TurnStartBlock->钗(7/远古)+山铜(6/罕见), TurnStartEnergy->贤者之石+祝福鹿角(远古), TurnStartDraw->异蛇之眼+小提琴(远古), PlayBlock->风之女(3/罕见)+音叉(4/罕见), PassiveMaxEnergy->灯笼(1/Common), StartEnergy->轰鸣海螺(远古).
- **未映射 (空数组)**: StartDamageAll, StartVulnAll, StartWeakAll, StartRegen, StartPoisonAll, StartPlating, StartArtifact (原版无直接对应遗物 - 悬停显示"不适用").
- `OurPointsFor(vref)` = CostPerPoint * 原版数值 (Magnitudes 表), 供悬停显示"我们的点数计价".
- **待扩充**: 负面词条全部无映射 (脆弱/失血/能量-/抽牌-/金币-/药水禁/懒惰/最大生命- 有原版对应, 如 钉刺颈环? 懒人药水? 未查); 额外池词条无映射.

### F. 点数预算编辑器 UI (新文件, 已提交, **未实机验证**)
- `mod/Code/Patches/BudgetEditorPanel.cs`: 每词条一行 = 效果文本 + 悬停原版遗物chips (NHoverTipSet.CreateAndShow/Remove, HoverTip(LocString,string)) + Min/Max 滑条 (NSlider, Godot.Range) + 每点计价标签 (live ChaosPointCosts).
- `RelicsSettingsSubmenu.BuildOptions`: BaseLib SetupConfigUI 之后追加编辑器 (header + panel).
- 配置: 94 个 Min_<T>/Max_<T> 属性 (BaseLib 持久化, 默认=catalog), `SetTemplateBounds`, `ApplyUserBounds` (SpecOf 应用).
- 已修 bug: (1) AddRows 用 ChaosRelicGenerator.SpecOf 而非 ChaosRelicCatalog.Spec (后者对 X_ 额外池模板抛异常); (2) 每点计价显示用 live PointCosts.
- **设计偏差 (用户原意未实现)**: 用户要求"上下限线段,左右端点是滑块" = **双端 range slider** (一条线段两端可拖). 当前实现是**两个独立 NSlider** (下限+上限). 需按原意改造 (自绘 Control: 线段 + 两个可拖手柄; 或查引擎有无现成 range slider).

## Pending (新会话任务)

1. **[优先级高] 实机验证预算编辑器**: 启动游戏 -> 设置 -> 常规 -> "东尼算法 - 遗物" -> 打开设置页. 检查: 页面渲染 (BaseLib 部分 + 编辑器部分), 悬停原版遗物 chip 弹窗 (描述/稀有度/我们的点数), 滑条拖动 + 持久化, 控制台报错. 方法: 启动游戏 (hub start), 日志确认 mod 加载, PowerShell 键鼠自动化导航 + 截图. 游戏已关闭可自由部署.
2. **双端滑条改造**: 按用户原意实现 Min/Max 一条线段两端滑块 (替换两个独立 NSlider).
3. **映射表扩充**: 负面词条 + 额外池词条的原版遗物引用 (查 `vanilla-relics-zhs.json` + `relic-values.json`).
4. **全面重定价复审**: 本会话只调了人工制品 (9). 用户要求按药水/卡牌基准重审**全部**词条 (如 StartStrength 5 vs 力量药水 2力量 - 是否仍合理; StartDexterity 4 vs 敏捷药水 2; 每回合效果 vs 一次性效果的比值; 衰减词条三角定价复核). 药水基准: 力量药水 2力量/Common, 敏捷药水 2敏捷/Common, 格挡药水? (待查), 再生药水? (待查).
5. **工坊发布**: 主 mod v0.5.x + RrcA4hKeyFix 新条目 (fileid 空). 推前问用户是否剥离 VDF description (网页手改保护). 涉及 `.tmp/workshop-push.ps1` + `.tmp/a4hkeyfix-push.ps1`; **绝不动 `.tmp/triple2-push.ps1`**.
6. DEVLOG 补记本会话 (Session 40+).

## 关键命令
```bash
# 构建 (游戏关闭可全量):
cd "G:/omp works/AutoAnthonyRelics/mod"
export DOTNET_CLI_HOME="G:/omp works/.dotnet-home" NUGET_PACKAGES="G:/omp works/sts2-spire1/.nuget/packages" NUGET_HTTP_CACHE_PATH="G:/omp works/sts2-spire1/.nuget/http-cache" TEMP="G:/omp works/.tmp" TMP="G:/omp works/.tmp"
dotnet build AutoAnthonyRelics.csproj   # 须含 "PCK packed"
# 部署三处:
#   g:/steam/steamapps/common/Slay the Spire 2/mods/AutoAnthonyRelics/  (主)
#   g:/steam/steamapps/common/Slay the Spire 2/mods_disabled/AutoAnthonyRelics/
#   G:/omp works/AutoAnthonyRelics/workshop/content/AutoAnthonyRelics/
# 游戏运行中构建加 -p:CopyToModsFolderOnBuild=false (dll 锁 MSB3027)
# 冒烟测试: G:/omp works/.tmp/budget-smoke/ (dotnet run smoke.csproj)
```
- 游戏主程序: `g:/steam/steamapps/common/Slay the Spire 2/SlayTheSpire2.exe`; 日志: `C:/Users/o_OBl/AppData/Roaming/SlayTheSpire2/logs/godot.log`.
- ilspycmd: `C:/Users/o_OBl/.dotnet/tools/ilspycmd.exe` (反编译 sts2.dll 等).
- 已部署最新构建 (0 warn 0 err + PCK packed), git 干净.

## 文件清单 (本轮相关)
- `mod/Code/Chaos/ChaosRelicCatalog.cs` - 词条文本+价格 (已对齐)
- `mod/Code/Chaos/ChaosRelicExtraCatalog.cs` - 额外池文本 (已对齐)
- `mod/Code/Chaos/VanillaRelicMapping.cs` - 原版遗物映射 (新)
- `mod/Code/Chaos/ChaosRelicGenerator.cs` - SpecOf 应用用户 bounds
- `mod/Code/AutoAnthonyRelicsConfig.cs` - Cost_C_START_ARTIFACT=9, 94 Min_/Max_ 属性, ApplyUserBounds
- `mod/Code/Patches/BudgetEditorPanel.cs` - 点数编辑器 (新)
- `mod/Code/Patches/RelicsSettingsSubmenu.cs` - 设置页集成编辑器
- `mod/Code/DevConsole/LocDumpConsoleCmd.cs` - locdump (新)
- loc: `mod/AutoAnthonyRelics/localization/{zhs,eng}/settings_ui.json` (BUDGET_* keys)
- 工具: `G:/omp works/.tmp/pck-extract/pckv3.py`, `G:/omp works/.tmp/send-locdump.ps1`
- 数据: `G:/omp works/.tmp/locdump/` (运行时 dump), `G:/omp works/.tmp/pck-extract/game-loc/localization/zhs/` (静态提取), `G:/omp works/.tmp/locdump/zhs-baseline.txt`, `vanilla-relics-zhs.json`, `relic-values.json`

## 联机确定性 (Tier-1 键, 两端必须一致)
预算 (Common/Uncommon/Rare), 负面概率, 每词条 Cost_/Refund_, EnableExtraPool, **新增: Min_/Max_ bounds**.
