# 断点交接 / Breakpoint Handoff - 2026-09-12

迁移目标: 另一个 agent 接手。本文档自包含,不需要任何聊天上下文。

---

## 0. 用户最新指令 (最高优先级,尚未执行)

用户在本轮中断并给出新方向。**当前工作已停止,只有本文档落盘。**

### 0.1 改名
- `东尼算法-遗物` → **`怪异炼化-遗物`** (当前这个"点数预算"风格更符合"怪异炼化"这个名字,即 Monster Hunter 的 qurious-crafting 风格)。
- 需要改的位置: mod manifest (`mod/AutoAnthonyRelics.json` 的 `name`)、loc 表 (`localization/{eng,zhs}/settings_ui.json`、`relics.json`)、`workshop/workshop_upload.vdf` 的 `title`/`description`、`workshop/DESCRIPTION*.txt`、`DEVELOP.md`、`DEVLOG.md`。
- 注意: mod **id 仍是 `AutoAnthonyRelics`**(`res://` 路径、cfg 文件名、MP 握手键都依赖它)。**只改显示名,不要改 id**——改 id 会作废用户已保存的 cfg 和联机双方的一致性校验。

### 0.2 另起一个新 mod: 真正的 `东尼算法-遗物`
用户要求**思想重写**一个新 mod,忠于原版东尼算法(AutoAnthony)的机制。

原版机制(用户原话要点):
- 它**拆分卡牌的效果**,然后**条件重组**,而不是简单打乱词条。
- 例子: 某张牌有"每在你的回合失去生命,获得2力量"这样的**条件触发词条**;东尼算法可能把它重组为"每在你的回合失去生命,失去1点生命"——**这是完全合法的输出**。
- 新 mod 必须复原"打乱条件"这个特点。

**可用的原版代码(已定位,未读取)**:
- `g:/steam/steamapps/workshop/content/2868840/3786611028/AutoAnthony.dll` (6.27 MB)
- 同目录 `AutoAnthony.json` (manifest)、`AutoAnthony.pck`
- 反编译工具: `C:/Users/o_Obl/.dotnet/tools/ilspycmd.exe` (9.1)
- 建议: `ilspycmd -o <outdir> AutoAnthony.dll` 后检索关键词 `Condition`、`Trigger`、`Recombine`、`Split`、`Chaos`、`Shuffle`。

### 0.3 额外池新机制
用户指出: 遗物有"在 X 场战斗后失效"、"可在火堆添火"这两个特性。而类似的特性 **"在 5 场战斗后从你的牌组中移除"** 来自诅咒 **"悔恨" (Guilty / Remorse)**,它**似乎没有进入东尼算法的随机池**。
- 用户要求: **为这个额外机制做额外池** (extra pool)。
- 引擎内已确认的实现参考: `MegaCrit.Sts2.Core.Models.Cards/Guilty.cs`
  - `[SavedProperty] public int CombatsSeen` (第 25-38 行)
  - `CombatsSeen++; if (CombatsSeen >= 5 && base.Pile.Type == PileType.Deck) await CardPileCmd.RemoveFromDeck(this);` (第 49-54 行)
  - setter 里同步 `base.DynamicVars["Combats"].BaseValue = 5 - CombatsSeen;`

### 0.4 用户回报的测试结果(实机)
- 能渲染 ✅
- 拖动正常 ✅
- 存盘、重启正常 ✅
- 悬浮文字、术语均正常 ✅

### 0.5 用户报告的缺陷(计划外,待修)
**(A) 大量文本显示为变量名,不是中文,甚至不是自然语言。**

**根因已定位并验证** (这是本轮最重要的发现):

BaseLib 的 `ModConfig.GetLabelText` 会对属性名跑 `StringHelper.Slugify`,然后查 loc 表 `{ModPrefix}{slug}.title`,查不到就**回退显示原始属性名**。

- 代码: `G:/omp works/sts2-spire1/research/BaseLib-StS2/Config/ModConfig.cs:499-503`
  ```csharp
  protected string GetLabelText(string labelName, bool slugify)
  {
      var key = slugify ? StringHelper.Slugify(labelName) : labelName;
      var loc = LocString.GetIfExists("settings_ui", $"{ModPrefix}{key}.title");
      return loc != null ? loc.GetFormattedText() : labelName;
  }
  ```
- `Slugify` 实现在 `MegaCrit.Sts2.Core.Helpers/StringHelper.cs:95-100`:
  正则 `([A-Za-z0-9]|\G(?!^))([A-Z])` → `$1_$2`,然后大写化、空白转 `_`、删除 `[^A-Z0-9_]`。
- **实测结果**(已用 Python 复现该正则,与 C# 行为一致):

  | 属性名 (loc 表里的键) | Slugify 之后 (BaseLib 实际查找的键) |
  |---|---|
  | `Cost_C_START_DAMAGE_ALL` | `COST_C_S_TA_R_T_D_AM_AG_E_A_L_L` |
  | `Refund_N_START_FRAIL_SELF` | `REFUND_N_S_TA_R_T_F_RA_I_L_S_EL_F` |
  | `Cost_X_HAND_RETAIN` | `COST_X_H_AN_D_R_ET_AI_N` |
  | `BUDGET_SECTION` | `B_UD_GE_T_S_EC_TI_O_N` |
  | `ChaosRelicBudgetCommon` | `CHAOS_RELIC_BUDGET_COMMON` ✅ 正常 |
  | `EnableChaosRelics` | `ENABLE_CHAOS_RELICS` ✅ 正常 |

  结论: **驼峰命名的属性名(如 `EnableChaosRelics`)能正确解析**,但**下划线命名且含连续大写的属性名(所有 `Cost_*` / `Refund_*` / `Min_*` / `Max_*`)全部解析失败**,于是 UI 直接显示 `Cost_C_START_DAMAGE_ALL` 这种变量名。

- 同样的机制影响 hover tip: `NConfigOptionRow.AddHoverTip()` (`BaseLib-StS2/Config/UI/NConfigOptionRow.cs:65-86`) 也用 `StringHelper.Slugify(Name)` 拼 `.hover.desc` / `.hover.title`。
- 也影响 slider 显示格式: `NConfigSlider.ResolveDisplayFormat` (`BaseLib-StS2/Config/UI/NConfigSlider.cs:169-176`) 拼 `.sliderFormat`。

**两种修法,推荐第 2 种:**
1. 把 loc 表里的键改成 slug 后的形态 (如 `AUTOANTHONYRELICS-COST_C_S_TA_R_T_D_AM_AG_E_A_L_L.title`)。缺点: 键名不可读,以后极易再次错位。
2. **重命名配置属性**为 Slugify 不动点 (slug 后等于自身),例如 `Cost_C_START_DAMAGE_ALL` → `CostStartDamageAll`。
   代价: `AutoAnthonyRelicsConfig.cs` 里 `LookUpCost`/`LookUpRefund`/`SetBoundProp` 拼的是 `"Cost_" + template` 和 `"Min_" + template`,必须同步改成 `"Cost" + PascalCaseOfTemplate` 之类的映射;同时 cfg 文件里已存的 `Cost_*` 键会失效(需写迁移或接受重置为默认)。
   注意: `Min_*`/`Max_*` 带 `[ConfigHideInUI]`,不影响 UI,但**影响 cfg 持久化和预算编辑器读写**,改名要一起处理。

**(B) 点数预算编辑器里,效果文本中的数值不应是具体数字,应为 `N`;下方滑条是 N 的取值。**

现状: `BudgetEditorPanel.EffectText` 调用 `ChaosRelicGenerator.RenderOperation(spec, spec.Max)` (`mod/Code/Patches/BudgetEditorPanel.cs:181-182`),即**用 Max 值把 `{N}` 替换成具体数字**。
- 期望: 显示 `{N}` 原样 (或字面 `N`),滑条决定 N。
- 相关: `ChaosRelicCatalog.TemplateSpec.Render(int amount)` = `TextPattern.Replace("{N}", amount.ToString())`。

**(C) 懒惰(sloth)应显示 `(7-N)`,而不是具体数字。**

现状: `ChaosRelicGenerator.RenderOperation` (`mod/Code/Chaos/ChaosRelicGenerator.cs:231-234`):
```csharp
internal static string RenderOperation(ChaosRelicCatalog.TemplateSpec spec, int amount) =>
    spec.Template == ChaosRelicCatalog.NegStartSloth
        ? spec.TextPattern.Replace("{M}", Math.Max(1, 7 - amount).ToString())
        : spec.Render(amount);
```
- 文本模板: `"每回合你无法打出超过{M}张牌."` (`ChaosRelicCatalog.cs:132`)
- 用户要求: 编辑器里显示 `(7-N)张牌`,滑条取 N。
- **注意区分**: 生成出来的遗物实际描述里仍应显示具体数字(那是运行时效果);只有**编辑器行**要显示 `(7-N)` 形式。当前 `RenderOperation` 被生成器和编辑器共用,改的时候要拆开或加参数。

**(D) 用户提问: 事件中获得的遗物没有被重生成吗?**

**已确认的答案(证据充分)**:
`ChaosRelicPoolReplacement` 打的是 relic bag 的 `Populate` 重载后置补丁,替换的是**随机池的 deque**。而 `RelicCmd.Obtain<T>(player)` / `Obtain(RelicModel, player, index)` (`MegaCrit.Sts2.Core.Commands/RelicCmd.cs:22,35`) 是**直接授予指定遗物**,绕过池子。所以:
- 事件里**固定授予**的遗物(如 AFTP 的 `ACTSFROMTHEPAST-BLOOD_BANK`)保持原版,不会被替换 —— 这是**设计如此**,见 `ChaosRelicPoolReplacementPatch.cs` 的注释: "Fixed event grants of specific relics (event content, not pool pulls)"。
- 从池子里**随机抽**的事件奖励(如事件里"获得一件随机稀有遗物")走的是 bag,会被替换。

如果用户希望事件固定授予的遗物也变成混沌遗物,那是**新需求**,需要额外 patch `RelicCmd.Obtain` 路径 —— 但要注意这会让 AFTP 这类依赖特定遗物的事件逻辑失效(例如 `Vampires.cs` 里 `Owner.Relics.First(r => r is BloodVial)` 会抛异常)。

---

## 1. 仓库状态

| 仓库 | HEAD | 未推送 | 工作区 |
|---|---|---|---|
| `G:/omp works/AutoAnthonyRelics` | `3323348` | 0 | **有未提交改动** (见下) |
| `G:/omp works/sts2-spire1` | `492f0e5` | 0 | clean |
| `G:/omp works/sts2-mpconfigsync` | `c5ef568` | 0 | clean |
| `G:/omp works/sts2-perfect` | `95cd793` | 0 | clean |
| `G:/omp works/sts2-heartshake` | `99dec48` | 0 | clean |
| `G:/omp works/sts2-regentfxfastboot` | `afa6e60` | 0 | clean |
| `G:/omp works/chaosbridge` | `9d9ad4a` | 0 | clean |
| `G:/omp works/aftp-ActsFromThePast` | clean | 0 | clean |

`AutoAnthonyRelics` 未提交改动:
```
 M DEVELOP.md
 M workshop/content/AutoAnthonyRelics/AutoAnthonyRelics.dll
 M workshop/content/AutoAnthonyRelics/AutoAnthonyRelics.pck
```
(后两个是 F09 之后重建的产物,需要提交或确认。)

**`sts2-boottimer` 不是 git 仓库。** `aftp-stage/`、`mod/` 也不是。

---

## 2. 本轮已完成并验证的工作

### 2.1 AutoAnthonyRelics F01/F02/F03/F06/F07/F08 已修复、构建、部署
- 新增 `mod/Code/Chaos/ChaosTemplates.cs`: 跨池解析器 (`Has`/`Spec`/`Effective`/`IsNegative`/`PositiveTemplates`/`NegativeTemplates`/`WatcherModLoaded`/`CheapestPositiveUnit`/`CheapestPositiveFloor`/`PriceOf`)。
- `ChaosPointCosts.CostPerPoint`/`RefundPerPoint` 走 `ChaosTemplates.Spec`(之前只查核心池,`EnableExtraPool=true` 时抛 `InvalidOperationException`)。**注意: 见 0.5(A),这里的 `Cost_`/`Refund_` 属性名正是 slug 问题的来源。**
- 94 个 bounds 属性改名为 template-id 形式 (`Min_C_START_STRENGTH`) + `[ConfigHideInUI]`;`ApplyUserBounds`/`SetTemplateBounds`/`SetBoundProp`/`LookUpInt` 基于一次性 `IntPropsByName` 索引。
- `ChaosRelicGenerator`: `UniqueOnly` 真正生效;预算下限抬升;保底正词条;`RenderOperation` 生成器/编辑器共用。
- `ChaosRelicRunRegistry` 缓存键 = seed + config 指纹。
- `ChaosRelicModel.cs` 重写: 能量延迟到 `AfterSideTurnStart`;战斗开始抽牌走 turn-1 `ModifyHandDraw`;`N_TURN_LOSE_HP` 用 `Unblockable|Unpowered`;`ModifyDamageAdditive` 加 `IsPoweredAttack` 门;retain buff 在 `AfterAttack` 消耗;`AfterCardPlayed` 自门控 owner;`ShowCounter`/`DisplayAmount`;附魔在首个玩家回合跑;retain 触发移到 `AfterFlush`;战斗结束清理额外池状态。
- 新增 `mod/Code/Patches/RangeSlider.cs` (单轨双手柄;引擎 `NSlider._Ready` 要求 `%Handle` 子节点)。
- `BudgetEditorPanel.cs` / `RelicsSettingsSubmenu.cs` 重写: 通过 `ModConfigRegistry.Get(MainFile.ModId)` 拿注册实例,BaseLib 式防抖保存 + `OnSubmenuHidden` 落盘。
- `RrcTreasureKeyCompat.TryInstall`: 用 `InstallGate` 锁 + `_installed` 快路径 + `Harmony.GetPatchInfo(original)` 按声明类型 FullName 匹配。
- 构建: `mod/AutoAnthonyRelics.csproj` 0 warning / 0 error,输出 `PCK packed`;`standalone/RrcA4hKeyFix.csproj` 0/0。
- 提交: `13ff225`, `5935f84`, `cf890f8`, `19c8e35`, `8c4f1b0`, `d007ef0`, `3323348`,**全部已推送**。

### 2.2 探针全部翻绿
对比 `assessment-2026-09-12-results.txt` (修复前) 与 `probe-2026-09-12-after-fix.txt` (修复后):

| 断言 | 前 | 后 |
|---|---|---|
| `boundKeys` | 0/94 | 94/94 |
| `boundsAfterEditorCall` | [1,10] | [2,2] |
| `extraPool` | 抛异常 | 完成 |
| `sameSeedConfigChange` cacheUnchanged | true | false |
| `legalUnaffordableConfig` emptyRelics | 60 | 0 |
| `unpoweredDamageBonus` | 3 | 0 |
| `displayCounter` shown | 0 | 2 |
| `damageQueryConsumesRetainBonus` | 7/3/0 | 4/4/4 |
| `retainedBonusAfterCombatEnd` | 4 | 0 |
| `slothEditorText` | 占位符 | 真实数字 |
| 新增 `cheapestFloor` | — | 20 |

探针源码 `G:/omp works/.tmp/aar-assessment-20260912/Program.cs`,运行:
```bash
cd "G:/omp works/.tmp/aar-assessment-20260912"
dotnet run --project Probe.csproj --no-restore
```

### 2.3 实机 autoslay 冒烟通过
两次运行 (`AARFIX1`, `AARFIX2`): `SlayTheSpire2.exe --autoslay --seed <S> --log-file <path>`。
证据: manifest 读作 0.5.1、新 bounds 键被 BaseLib 持久化、种子捕获、60 槽描述刷新、池替换(移除 236+244 原版遗物)、混沌遗物在 14 场战斗中持有且零 `[ERROR] [AutoAnthonyRelics]`、Sapphire Key 授予。
F06 实机验证: `AutoAnthonyRelics` 日志 "active: .. OnSkipped patched",`RrcA4hKeyFix` 日志 "initialized dormant"。
日志在 `G:/omp works/AutoAnthonyRelics/.tmp/`。

### 2.4 宝箱房失败的 A/B 隔离
两次运行都死在 `TreasureRoomHandler` 的 `Proceed button not enabled after picking relics`。
关掉 `EnableChaosRelics=False` 重跑: 池替换数 0,三个宝箱全被第三方 `RelicRewardChoices` 接管(候选是纯原版 VAMBRACE/ORICHALCUM/ORNAMENTAL_FAN),**同样失败**。
结论: **不是我们的缺陷**。证据 `.tmp/autoslay-AARFIX2-off.log` 与 `.tmp/SMOKE-2026-09-12.md`。

### 2.5 跨项目修复已提交推送
- `sts2-regentfxfastboot` + `sts2-boottimer`: `mod/project.godot` 的 `config/name` 和 `assembly_name` 是残留的 `"Perfect"`;`GlobalUsings.cs` 注释写成 MpConfigSync。regentfx `afa6e60` 重建 0 error;boottimer 非 git 仓库,本地改并重建 0 error。
- `sts2-spire1`: manifest description + workshop VDF description/changenote 从陈旧的 v1.0.0 计数同步到 v1.1.0 (230 cards / 22 relics / 6 exclusive events) (`396806e`);新增 `dist/REBUILD-PENDING.md` 记录朋友包积压与已验证哈希 (`492f0e5`)。
- `sts2-heartshake` (`99dec48`)、`sts2-mpconfigsync` (`c5ef568`)、`chaosbridge` (`9d9ad4a`)、`sts2-perfect` (`95cd793`) 文档漂移已修。
- 各项目调研报告: `agent://SurveySpire1` 等。

### 2.6 F09 文档已交付
`DEVELOP.md`、`workshop/TEMPLATE-POINTS-LIST.md`、全部 workshop 描述、`workshop/workshop_upload.vdf`、`mod/AutoAnthonyRelics.json` 0.5.0→0.5.1、两个 `settings_ui.json`(术语 人工制品/懒惰/锋利/灵巧/注能;`BUDGET_MIN`/`BUDGET_MAX` 移除,`BUDGET_RANGE` 新增)。之后重建并重新部署。

---

## 3. 本轮新发现的重大结论 (MpConfigSync 审计 + 联机分歧重审)

这一节是用户明确要求的两个问题的答案,**已用证据闭环**。

### 3.1 用户问题一: "Astra 之前指出配置推送 mod 在加载阶段有问题?"

**结论: 未复现 Astra 所指的"加载阶段"缺陷。但审计发现 3 个真实缺陷(见 3.3)。**

已排除的假设(逐条验证):
- **`WrapperMessageId` 不一致**: 排除。`MessageTypes.Initialize()` 在 `OneTimeInitialization.cs:84` 执行(早于 mod 加载);BaseLib 的 `AdjustCustomMessageKeys` 后置补丁 (`BaseLib-StS2/Patches/Networking/CustomMessagePatches.cs:36-72`) 扫描 128 起的空闲字节。基类 `INetMessageSubtypes` 只有 **52 个**类型,远低于 128。**实机日志已确认双方都是 `Using IDs 128 and 129`**。
- **`idDatabaseHash` / SavedProperty 网表不一致**: 排除。分歧报告里 `savedProperties.mapHash: local=0x3A5629E4; remote=0x3A5629E4` **完全相同**,`net idBitSize` 都是 7,`count` 都是 110,`net idMap` 的 0000-0109 逐行一致。
- **mod 集合差异被跳过**: 属实且是设计如此。`Spire1Config.IgnoreMpModDifferences` 默认 `true`(`Spire1Config.cs:48`,补丁 `Spire1Code/Patches/MpIgnoreModDiffPatch.cs`)。2026-09-11 的分歧包中主机 36 个 mod、对端 26 个,握手却通过了 —— 就是这个开关在起作用。

### 3.2 用户问题二: "反复出现的联机分歧是不是事件/古代遗物被 modified 但没同步给所有人?"

**结论: 是,但根因是"本地配置导致的选项分支不同",不是遗物同步机制坏了。共两个独立根因。**

已提取 5 个分歧包到 `G:/omp works/.tmp/divzip/`(每个含 `state-divergence-report.txt`、`metadata.json`、`local-debug-log.records.json`、`remote-debug-log.records.json`)。

**根因 1: AFTP `RebalancedModeEffective` 未覆盖 `Vampires` / `WindingHalls` 两个事件。**

- 两个 VAMPIRES 包 (checksum 411 / 8) 的唯一实质差异: 主机四个玩家都有 `RELIC.ACTSFROMTHEPAST-BLOOD_BANK`(floor=26),对端四人全无。
- 代码路径: `G:/omp works/aftp-ActsFromThePast/ActsFromThePast/Acts/TheCity/Events/Vampires.cs:52-55`
  ```csharp
  if (ActsFromThePastConfig.RebalancedModeEffective)
      options.Add(Option(Hesitate, "INITIAL_REBALANCED", HoverTipFactory.FromRelic<BloodBank>().ToArray()));
  else
      options.Add(Option(Leave));
  ```
  同一个选项**索引**在两种模式下指向不同动作,于是主机点了"犹豫"(给 BloodBank),对端把同一索引解释成"离开"。
- 我们的 fork 已经给 `RebalancedMode` 加了 MP 保护 (`ActsFromThePastConfig.cs:14-19`,注释里的 "fork fix 2026-08-27"),但**该保护只在 `RebalancedModeEffective` 里**,`Vampires.cs` 与 `WindingHalls.cs` 确实都用了 `Effective` 版本 —— 所以这两个文件本身是安全的。
- **真正的问题**: 分歧包里主机加载的是 **workshop 版 AFTP v1.0.5**,而我们的 fork 是 **1.0.6**。需要确认 workshop 版是否已包含该保护;若未包含,需要重新发布我们的 fork。

**根因 2: 原版遗物的 `[SavedProperty]` 计数器在两个客户端之间漂移 ±1。**

两个 WINDING_HALLS 包 (checksum 322 / 8) 的行级 diff 只找出一处真实差异(其余"index 00 不匹配"是报告只打印 `id`/`floor`/`props` 造成的假象):

| 遗物 | 主机 | 对端 |
|---|---|---|
| `BOOK_OF_FIVE_RINGS` | `CardsAdded=15` | `CardsAdded=16` |
| `BOOK_OF_FIVE_RINGS` | `CardsAdded=14` | `CardsAdded=15` |
| `PUMPKIN_CANDLE` | `KindleCount=5` | `KindleCount=6` |
| `TOY_BOX` | `CombatsSeen=10` | `CombatsSeen=9` |
| `PENDULUM` | `TurnsSeen=0` | `TurnsSeen=2` |
| `LASTING_CANDY` | `CombatRewardsSeen=12` | `CombatRewardsSeen=11` |

这些都是**原版遗物**,不是 AFTP 遗物,也不是我们的 mod。引擎源码位置:
- `BookOfFiveRings.cs:85-91` — `AfterCardChangedPiles` 里 `CardsAdded++`
- `Pendulum.cs:81-87` — `BeforeHandDraw` 里 `TurnsSeen = (TurnsSeen + 1) % Turns`
- `PumpkinCandle.cs:73,89` — `KindleCount = Math.Max(KindleCount - 1, 0)` / `KindleCount += 5`
- `ToyBox.cs:105-109` — `CombatsSeen++`
- `LastingCandy` — `CombatRewardsSeen`(注意 `SerializableRunV16ToV17.cs` 里有 `CombatsSeen`→`CombatRewardsSeen` 的迁移)

**这是引擎的 MP 同步缺陷**(`[SavedProperty]` 计数器只在本地递增,没有走 `NetFullCombatState` 的同步路径),**不是我们任何一个 mod 的 bug**。需要向用户明确说明: 这类 ±1 漂移无法由 mod 侧修复,除非 hook 引擎的计数器写入路径并强制同步。

**根因 3 (级联,非根因):** `checksum #43` 的 75 个差异字段是上面漂移的**后果**——`creatures[0].powers` 多出 `RETAIN_HAND_POWER`、`VIGOR_POWER:8` 丢失、`creatures[5].identity` NEMESIS vs JAW_WORM、五条 RNG 流全部不同。不要把它当独立问题查。

**mod 集合差异(非根因,已确认无害):** 主机比人多 10 个 mod,其中只有 `MpConfigSync` 是 gameplay 标记,其余全是 non-gameplay。被 `IgnoreMpModDifferences` 跳过。

### 3.3 MpConfigSync 自身发现的 3 个真实缺陷(待修)

1. **`MainFile.cs:31-38` 注释与代码矛盾。** 注释说 "applied per-type with try/catch - never PatchAll",代码实际调用 `harmony.PatchAll(Assembly.GetExecutingAssembly())` 包在单个 try/catch 里。要么改注释,要么改成逐类型打补丁。

2. **`ConfigPropertyScanner.Scan` 的谓词与它声称镜像的 BaseLib 不一致。**
   - 我们: `BindingFlags.Public | BindingFlags.Static | BindingFlags.FlattenHierarchy`
   - BaseLib (`ModConfig.CheckConfigProperties`, `ModConfig.cs:143-150`): `configType.GetProperties()`(public 实例+静态)**然后**过滤 `GetMethod?.IsStatic != true`。
   - `FlattenHierarchy` 与 `GetProperties()` 在**继承的静态属性**上语义不同(前者不返回基类静态成员)。需要对齐或明确记录差异。

3. **`ConfigSyncMessage.ShouldBuffer` 用的是 `ICustomMessage` 默认值 `true`(未覆写)。**
   加载阶段的运行启动消息会被缓冲到 `RunManager.Launch()` 才释放。需要确认这对配置推送是否合适 —— 主机在 `InitializeShared` 后置补丁里发送,此时缓冲**已开启**(`StartRunLobby.cs:498` / `LoadRunLobby.cs:320`),释放点在 `RunManager.cs:711-717` 的 `SetBufferMessages(false)`。配置推送被缓冲是**可接受的**(接收端在 Launch 后才应用,不影响 `InitializeShared` 期间构建的同步器),但要在文档里写清楚。

另需确认:
- `RunManager.CleanUpPatch` 的恢复路径是否覆盖全部结束方式(放弃/死亡/胜利/返回菜单/断线)。`RunManagerCleanUpPatch.cs` 的注释声称都覆盖,但未验证。
- `RunManagerInitializeSharedPatch` 与 BaseLib 的 `InitializeShared` 后置补丁**顺序不确定**(BaseLib 在它自己的后置里注册 handler)。当前注释声称顺序无关(因为 `Send` 只需要 `NetService` 已赋值 + `CustomMessageWrapper.Initialize()` 已在启动时跑过),这个论证看起来成立,但未实测。

---

## 4. 其他未完成项

- **F05**(原版遗物基线重建)子代理 `ImplF05Baseline` 仍在运行。目标文件 `mod/Code/Chaos/VanillaRelicMapping.cs`(当前 153 行,尚无精度标记)。必须编码的已验证事实:
  - `DaughterOfTheWind` 是 `RelicRarity.Event`,`BlockVar(1m, Unpowered)` 每张攻击牌(表里写的是 罕见/3)。
  - `TuningFork` 是 Uncommon,每 10 张技能牌 7 点格挡(表里写的是 4 per 3)。
  - `RingOfTheSnake` 是 `RelicRarity.Starter`。
  - `BeltBuckle` 是 `RelicRarity.Shop`,其 2 敏捷以"不持有药水"为条件。
  - `Lantern` 是首回合一次性能量,**不能**呈现为持续的 `PASSIVE_MAX_ENERGY` 等价物。
  - `OurPointsFor` 必须用 `AutoAnthonyRelicsConfig.PointCosts.CostPerPoint` 并遵守 `Decaying` 三角计价。
  - 可安全复制的伤害模式: `StrikeDummy`/`MiniatureCannon` 门控 `props.IsPoweredAttack()`、`cardSource == null`、卡牌谓词,然后 `dealer != Owner.Creature && cardSource.Owner != Owner`。
- **仍在运行的审查子代理**: `ReviewContract`、`ReviewAarFix`、`ReviewHeartshake`、`ReviewMpConfigSync`、`ReviewPerfect`、`ReviewChaosBridge`。注意: 新会话里这些 ID 可能已失效,需要通过 `agent://<id>` 或 `history://<id>` 取。
- **`sts2-spire1` v1.1.0** 已构建并暂存于 `workshop/content/Spire1/`(dll 459776 B,pck 29577930 B),**未发布**;workshop fileid `3799031900`。发布需要 Steam Guard 验证码,且用户需先决定是否剥掉 VDF 的 `description` 字段。
- **`RrcA4hKeyFix`** 独立包尚无 workshop fileid(暂存于 `workshop-keyfix/`)。
- `sts2-regentfxfastboot` 遗留 3 个构建警告(2× CS0436 来自 publicize 引用的 `RegentFX.dll`,1× STS002 本地化附加文件);`sts2-boottimer` 遗留 1 个(STS002)。均为既有且无害。

---

## 5. 关键决策与约定

- **保留 AutoAnthonyRelics 架构**(Config → Catalog/PointCosts → Generator(60 槽) → RunRegistry → ChaosRelicModel/LocUpdater → 池替换 + 引擎钩子)。不重写。
- **先修契约再调价。** 在 F05 用当前引擎重建 `VanillaRelicMapping` 之前,不要基于它做调价。不要改动用户已裁定的人工制品 9 点定价。
- **F08 退化配置契约**(主会话选定,用户未确认): 每个生成遗物至少一条正词条;配置预算低于 `ChaosTemplates.CheapestPositiveFloor` 时在生成器内部抬升到下限。编辑器滑条不动。**需告知用户,若反对则回滚。**
- **"编译并部署" ≠ "已实现"。** 旧交接里"已部署三处"的说法对新功能一律视为未验证。
- **用探针而非假设。** AutoAnthonyRelics 的所有结论都有隔离探针支撑。
- **实机失败必须先隔离再归咎于我们的 mod。** 宝箱房失败是在关掉本 mod 复现之后才归给 `RelicRewardChoices` 的。
- **孪生包的去重是跨程序集的**,不是按程序集: 用 `Harmony.GetPatchInfo`(进程级 `HarmonySharedState`)按声明类型 FullName 匹配。

---

## 6. 环境与命令

### 构建(游戏需关闭,或加 no-copy 标志)
```bash
cd "G:/omp works/AutoAnthonyRelics/mod"
export DOTNET_CLI_HOME="G:/omp works/.dotnet-home" \
       NUGET_PACKAGES="G:/omp works/sts2-spire1/.nuget/packages" \
       NUGET_HTTP_CACHE_PATH="G:/omp works/sts2-spire1/.nuget/http-cache" \
       TEMP="G:/omp works/.tmp" TMP="G:/omp works/.tmp"
dotnet build AutoAnthonyRelics.csproj                                   # 必须打印 "PCK packed"
dotnet build AutoAnthonyRelics.csproj -p:CopyToModsFolderOnBuild=false  # 游戏占用 dll 时

cd "G:/omp works/AutoAnthonyRelics/standalone/RrcA4hKeyFix"
dotnet build RrcA4hKeyFix.csproj --no-restore -p:CopyToModsFolderOnBuild=false
```

### 部署目标(三处必须同步)
- `g:/steam/steamapps/common/Slay the Spire 2/mods/AutoAnthonyRelics/`
- `g:/steam/steamapps/common/Slay the Spire 2/mods_disabled/AutoAnthonyRelics/`
- `G:/omp works/AutoAnthonyRelics/workshop/content/AutoAnthonyRelics/`

孪生包: `g:/steam/steamapps/common/Slay the Spire 2/mods/RrcA4hKeyFix/`

### 游戏
- 二进制: `g:/steam/steamapps/common/Slay the Spire 2/SlayTheSpire2.exe`
- 日志: `C:/Users/o_Obl/AppData/Roaming/SlayTheSpire2/logs/godot.log`
- 配置: `C:/Users/o_Obl/AppData/Roaming/SlayTheSpire2/mod_configs/AutoAnthonyRelics.cfg`
- autoslay: `--autoslay --seed <S> --log-file <path>`。引擎在 `!NGame.IsReleaseGame()` 后门控(`NGame.cs:694`);`Spire1` 的 `AutoSlayGatePatch` 在有 `--autoslay` 时强制为 false,且已发布版 Spire1 dll 含该补丁。运行失败后游戏退出码 1。
- 反编译: `C:/Users/o_Obl/.dotnet/tools/ilspycmd.exe` (9.1)
- 引擎反编译源: `G:/omp works/sts2-spire1/research/engine-dllsrc/`,索引 `research/engine-api-index.md`
- BaseLib 源: `G:/omp works/sts2-spire1/research/BaseLib-StS2/`

### 机器约定
- **绝不写 C:** 缓存/构建/临时全部导到 `G:`。
- Git: `user.name "Twelve-eight"`,`user.email "Twelve-eight@users.noreply.github.com"`。代理不通时用 `git -c http.proxy= -c https.proxy= push origin master`。
- 不要动 `.tmp/triple2-push.ps1`(共享)。
- 未经明确授权,不要传送/停止/重启正在运行的客户端。
- 文本语言卫生: 提示词只允许中/英/法/德/俄 + ASCII。检查器 `G:/omp works/sts2-spire1/tools/check-agent-text.mjs` (`node <checker> --file <path>`)。
- 子代理只写代码,**跳过 build/lint/test**;主会话集中构建。每个写代码的子代理要在**同一批次**里配一个 `reviewer`,且 reviewer 必须等实现者报告后再审。
- `.omp/hooks/pre/backup.ts` 在 `AutoAnthonyRelics` 里**已存在**。

### 已知噪音(不要重复上报)
- `Asset not cached: res://images/packed/card_template/card_frame_sdf.exr`
- `Volume can't be set to NaN.`
- 退出时的 RID/shader 泄漏警告
- `[Perfect] Harmony patch failed (AFTP absent?)`
- `[AutoAnthony] Expected 65 complete v111 Colorless cards, found 77`
- 宝箱房 `Proceed button not enabled after picking relics`(属 `RelicRewardChoices`,已 A/B 证实)

### 工具陷阱
- 用 heredoc Python 读本仓库的 `.txt`(CRLF + 特定内容)会**静默截断**。改用 `bash` 层 `grep`/`sed`/`awk`,或显式 `io.open(..., encoding='utf-8', errors='replace')` 读取。
- `grep` 工具对 CRLF 行可能匹配不到(见 `relic-bag` vs `relic-bag` 的教训)。分歧报告里字段名是 `relic-bag`。
- 从会话 jsonl 里抽长字符串也会静默截断,切成 <800 字符的块。
- PowerShell 写 `.java`/文本会加 BOM,导致 javac 报 illegal character;用 Write 工具或 `-Encoding ASCII`。

---

## 7. 子代理中止与抢救 (2026-09-12,用户下令强制结束)

用户下令停止全部工作并强制结束子代理.共 7 个在跑,处置与产出如下.

### 7.1 `ImplF05Baseline` - 完整交付(仅验证,零代码)

**这是唯一完整产出**: 状态 `completed`,完整报告在 `agent://ImplF05Baseline`,镜像 `C:/Users/o_Obl/.omp/agent/sessions/--G--omp works--/2026-09-11T18-59-25-820Z_01a091d6-c1fc-77db-ad4b-0cdbfbd6e8ac/ImplF05Baseline.md`.

**关键: 它一个字节都没写.`mod/Code/Chaos/VanillaRelicMapping.cs` 仍是旧的 153 行版本(已验证:8459 字节,时间戳 Sep 11 05:30).**

它完成的是**替换数据集的验证**,全部可直接用于重写:

- **引擎锚点**: `G:/steam/steamapps/common/Slay the Spire 2/release_info.json` = commit `41cef1ea`,version `v0.111.0`,date 2026-08-13,`main_assembly_hash` 222455745.`md5sum data_sts2_windows_x86_64/sts2.dll` = `a91f9e7be785f504205313f3aff55854`.
- **稀有度中文标签**(从 pck 内 `localization/zhs/gameplay_ui.json` 提取,4836 字节): `RELIC_RARITY.STARTER`=初始遗物, `.COMMON`=普通遗物, `.UNCOMMON`=罕见遗物, `.RARE`=稀有遗物, `.SHOP`=商店遗物, `.EVENT`=事件遗物, `.ANCIENT`=**先古**遗物.旧表里的"远古"在引擎里**根本不存在**.
- **必须修正的矛盾**(源在 `sts2-spire1/research/engine-dllsrc/MegaCrit.Sts2.Core.Models.Relics/`):
  | 遗物 | 引擎事实 | 旧表错误 |
  |---|---|---|
  | `DaughterOfTheWind` | `Rarity.Event` + `BlockVar(1m)`,按攻击牌触发 | 罕见, 每次攻击 3 |
  | `TuningFork` | `Uncommon` + `CardsVar(10)` + `BlockVar(7m)` | 罕见, 每 3 张技能 4 |
  | `RingOfTheSnake` | `Starter` + `CardsVar(2)` 仅首回合 | 普通 |
  | `BeltBuckle` | `Shop` + `DexterityPower(2m)`,以"不持有药水"为条件 | 普通, 无条件 |
  | `Lantern` | `Common` + `EnergyVar(1)` 仅 `turn<=1` 一次性 | 误映射为 `PASSIVE_MAX_ENERGY` |
  | `EmberTea` | `StrengthPower(2m)`,`private int _combatsLeft = 5;` | 前 2 场 |
  | `PhilosophersStone` / `BlessedAntler` | `Ancient`, `EnergyVar(1)`,都走 `ModifyMaxEnergy` | 归在 `TURN_START_ENERGY` |
  | 中文名 | 奥利哈钢 / 赐福鹿角 / 风的女儿 / 蛇之戒指 / 腰带扣 | 山铜 / 祝福鹿角 / 风之女 / 蛇之戒 / 皮带扣 |
- **原版数值表**(逐文件核对): `Anchor` Common `BlockVar(10m)`;`Vajra` Common `StrengthPower(1m)`;`SwordOfJade` Event `StrengthPower(3m)`;`OddlySmoothStone` Common `DexterityPower(1m)`;`BagOfPreparation` Common `CardsVar(2)` 首回合;`BronzeScales` Common `ThornsPower(3m)`;`BagOfMarbles` Common `VulnerablePower(1m)`;`RedMask` Common `WeakPower(1m)`;`Gorget` Common `PlatingPower(4m)`;`Sai` Ancient `BlockVar(7m)`;`Orichalcum` Uncommon `BlockVar(6m)`;`FakeOrichalcum` Event `BlockVar(3m)`;`SneckoEye` Ancient `CardsVar(2)`/回合;`Fiddle` Ancient `CardsVar(2)`/回合;`BoomingConch` Ancient `CardsVar(2)`+`EnergyVar(1)` 仅精英;`StrikeDummy` Common `ExtraDamage 3m`;`FakeStrikeDummy` Event `ExtraDamage 1m`;`MiniatureCannon` Uncommon `ExtraDamage 3m` 仅升级牌;`MysticLighter` Shop `DamageVar(9m, Unpowered)` 仅附魔牌;`BurningBlood` Starter `HealVar(6m)`;`BlackBlood` Starter `HealVar(12m)`;`MeatOnTheBone` Rare `HealVar(12m)` `HpThreshold 50`;`RegalPillow` Common `HealVar(15m)`;`BowlerHat` Uncommon `GoldIncrease 1.25m`;`FakeAnchor` Event `BlockVar(4m)`.
- **新确认**(补齐了之前标记的两个缺口): `LeafyPoultice.cs` = `new MaxHpVar(12m)`;`SereTalon.cs` = `new HpLossVar(9m)` + `new DynamicVar("Wishes", 3m)`;两者都是 Ancient 且都走 `AfterObtained`/`CreatureCmd.LoseMaxHp`.所以 `NegMaxHpDown` 有两个真实原版对应物(9 和 12).
- **每回合能量家族**: `Candelabra` Uncommon `EnergyVar(2)` 第 2 回合;`Chandelier` Rare `EnergyVar(3)` 第 3 回合;`VeryHotCocoa` Ancient `EnergyVar(4)` `turn<=1`;`VenerableTeaSet` Common `EnergyVar(2)` `AfterEnergyReset`;`ArtOfWar` Rare `EnergyVar(1)` 门控 `TurnNumber>1` + 上回合无攻击;`HappyFlower` Common `EnergyVar(1)` 每 `Turns=3`;`PaelsTears` Ancient `EnergyVar(2)` 门控剩余能量;`SealOfGold` Ancient `EnergyVar(1)`+`GoldVar(3)`;`VelvetChoker` Ancient `CardsVar(6)`+`EnergyVar(1)`;`Sozu` Ancient `EnergyVar(1)`,`ShouldProcurePotion` 返回 `player != Owner`;`Ectoplasm` Ancient `EnergyVar(1)`,`ModifyGoldGained` 返回 0m;`PaelsFlesh` Ancient `EnergyVar(1)`,`ModifyMaxEnergy` 门控 `TurnNumber<3`;`Bread` Shop `GainEnergy 1` / `LoseEnergy 2`.
- **抽牌 / 伤害 / 杂项**: `Pendulum` Common `CardsVar(1)` 每 `Turns=3`;`PollinousCore` Event `CardsVar(2)` 每 `Turns=4`;`Tingsha` Uncommon `DamageVar(3m)` 弃牌时;`Kusarigama` Uncommon `CardsVar(3)`+`DamageVar(6m)`;`LetterOpener` Uncommon `CardsVar(3)`+`DamageVar(5m)`;`OrnamentalFan` Uncommon `CardsVar(3)`+`BlockVar(4m)`;`BloodVial` Common `HealVar(2m)` `turn<=1`;`MrStruggles` Event;`ScreamingFlagon` Shop `DamageVar(20m)`;`SneckoSkull` Common `PoisonPower(1m)`;`UnsettlingLamp` Rare 使一个 debuff 翻倍(不给人工制品).
- **空组复核**(grep 过 `Relics/*.cs`): `RegenPower` 无;`FrailPower` 无;`ArtifactPower` 只有 `UnsettlingLamp`(翻倍 debuff);`ModifyBlockAdditive` 无;`ThornsPower` 只有 `BronzeScales`;`ModifyRestSiteHealAmount` 只有 `RegalPillow`;无任何"战斗胜利给金币"的遗物.
- **计价/消费面已确认**: `mod/Code/AutoAnthonyRelicsConfig.cs:381` `internal static ChaosPointCosts PointCosts`;`mod/Code/Chaos/ChaosRelicCatalog.cs:179` `public int CostPerPoint(string)`;`ChaosRelicCatalog.cs:153` `HasTemplate`;`ChaosRelicCatalog.cs:78-89` `TemplateSpec` 含 `bool Decaying`;`mod/Code/Chaos/ChaosTemplates.cs` 的 `PriceOf(spec, costs, amount)` = `Decaying ? per*n*(n+1)/2 : per*n`.**旧代码用的是 `spec.CostPerPoint * n`(catalog 默认值)而不是 live config 值 - 这就是缺陷.**
- **消费者面已确认**: `mod/Code/Patches/BudgetEditorPanel.cs:133` `For()`,`:217` `VanillaRef`,`:250` `OurPointsFor()`,`:254-257` 渲染 `DisplayName`/`Description`/`Rarity`/`EffectNote`;loc 键在 `mod/AutoAnthonyRelics/localization/zhs/settings_ui.json`(`BUDGET_RARITY`=稀有度,`BUDGET_OURCOST`=我们的点数计价: {P} 点).
- **副产品**: 引擎 zhs 的 ~45 个遗物标题+描述已渲染(BBCode 已剥离,变量已代入)在 `G:/omp works/.tmp/f05-verify/zhs-relics.json`(87795 字节).同目录还有 `eng-relics.json`(91491),`zhs-powers.json`(80850),`zhs-card_keywords.json`(885).

**它自己标记的未完成**: 约 6 条 zhs 字符串(`ECTOPLASM`, `PAELS_FLESH`, `BREAD`, `SERE_TALON`, `PENDULUM`, `POLLINOUS_CORE`, `FAKE_HAPPY_FLOWER`)渲染后仍带未代入的 `{Var}` 占位符;**原始字符串在提取物里,要重新渲染而不是照抄占位符形态**.

### 7.2 `ReviewAarFix` - 中止,部分分析已抢救

被强制取消,未产出最终报告.**它审的是修复前的快照**,而主会话的重写当时正在落盘(它自己在 thinking 里明确发现了这一点:"the main session is concurrently rewriting these files").因此它的部分结论已被修复覆盖.

**仍有效且值得跟进的发现**(来自 `history://ReviewAarFix` 的 thinking 块):

1. **`X_RETAIN_ATTACK_BUFF` 文案与实现时机不符.** 文案 `"每当你保留一张牌时,本回合你的下一张攻击牌伤害+{N}"`,但 buff 是在 `AfterFlush`(回合结束)发放的,所以实际生效于**下一回合**的下一张攻击牌,不是"本回合".需要改文案或改实现.
2. **多段攻击的消耗时机存疑.** 若一张攻击牌产生多个 `AttackCommand`(如横扫多敌),`ModifyDamageAdditive` 对每个敌人都加 buff(符合"下一张攻击牌"),但 `AfterAttack` 在**第一次**就清零 `_retainAttackBuff`.需对照原版 `VigorPower` 的模式确认命令与 `AfterAttack` 的交错顺序.
3. **`owner.PlayerCombatState?.TurnNumber <= 1` 的 null 语义.** 它当时在推敲 C# 提升运算符:`nullable <= int` 在 null 时返回 `false`.**需要实测确认**(若返回 true 则非战斗场景会误发能量).这一点它没得出结论.
4. **`PickAffordablePositive` 收到 `uniqueOnly` 参数却从不使用**(修复前状态).主会话的修复声称已让 `UniqueOnly` 真正生效 - **需要用当前代码复核该断言是否成立**.
5. **`SpendOnPositives` 的 `taken` 集合在 `amount < spec.Min` 时也会 `Add` 并 `continue`**,且 `while` 条件里 `taken.Count < ChaosTemplates.PositiveTemplates.Count` 把 `UniqueOnly` 模板也计入了上界(保守,无害,但值得注释).
6. **`VanillaRelicMapping.OurPointsFor`(`VanillaRelicMapping.cs:128`)直接调 `ChaosRelicCatalog.Spec`**,是残留的"仅核心池"查找.当前安全(`Map` 的键全是核心常量),但违反单一解析器契约,应改为走 `ChaosTemplates`.
7. **`OurPointsFor` 用 `spec.CostPerPoint`(catalog 默认值)而非 live config 值.** 与 F05 的发现一致(见 7.1).属修复前既有问题.

它**没有**完成的: 声明 8、9(`RangeSlider` 与 `BudgetEditorPanel` 的保存路径)只读到文件,未给出结论.

### 7.3 `ReviewContract` - 中止,仅元层面发现

被强制取消.**它的绝大部分产出是"文件在审查期间被改写"的困惑记录**,而非缺陷清单.可抢救的实质内容:

1. **确认了 94 个 `Min_*`/`Max_*` 属性的算术**: 核心正 26 + 核心负 10 + 额外正 10 + 额外负 1 = 47 模板 x 2 = 94.逐个核对无误.
2. **`ChaosTemplates.cs` 在主会话重写期间才出现** - 它的第一次 `read` 看到的是无 `ConfigFingerprint`、无 `RenderOperation`、无 `ChaosTemplates.cs` 的旧版本.这就是它后半段无法收敛的原因.
3. **它提出的唯一值得验证的机制问题**: `Dictionary<string, TemplateSpec>` 的**枚举顺序**是否在两个 MP 端一致.它当时正在查 .NET Core 的 `string.GetHashCode()` 是否按进程随机化.`ChaosRelicCatalog.Specs` 是 `new(StringComparer.Ordinal)`,`PositiveTemplates`/`NegativeTemplates` 由 `Specs.Values.Where(..).ToArray()` 派生,**顺序即字典枚举顺序**.若该顺序不确定,两个客户端的 `ActivePositiveTemplates` 顺序会不同,进而 `PickAffordablePositive` 的随机选择分歧 -> **MP 不同步**.
   **这是必须验证项,不是已确认缺陷.** 验证方法: 在两个进程里 dump `ChaosRelicCatalog.PositiveTemplates` 的顺序并比对;或直接把 `Specs` 改成按 `Template` 序排序的数组,从根上消除对字典顺序的依赖(推荐后者,因为它同时也让 `ConfigFingerprint` 稳定).
   注: .NET Core 的 `string.GetHashCode()` **确实**按进程随机化(除非设 `DOTNET_SYSTEM_GLOBALIZATION_INVARIANT` 之类的开关),但 `Dictionary` 只在扩容/删除时重排,插入顺序相同则枚举顺序在实践中稳定 - **"实践上稳定"不是契约,应消除该依赖**.

### 7.4 `ReviewHeartshake` / `ReviewMpConfigSync` / `ReviewPerfect` / `ReviewChaosBridge` - 零产出

这四个**连一次工具调用都没有发出**就被取消(transcript 里 `tool_execution_start` 计数 = 0,只有派发消息和两次中止消息).它们各自的任务书完整保留在 transcript 里,可直接重派:

- `ReviewHeartshake` - 审 `sts2-heartshake/mod/HeartShakeCode/**`.重点: `CombatRoomReadyPatch` 用字符串 `CORRUPT_HEART` 匹配怪物是否对字段/大小写正确(静默不匹配会让整个 mod 失效);`HeartBeatNode` 生命周期(重进房间泄漏 / 心死后仍触发 / 音频播放器重复启动);ogg 加载路径与 pck 打包路径是否一致、缺文件是否被闩锁;配置开关是否同时门控震动与音效.
- `ReviewMpConfigSync` - 审 `sts2-mpconfigsync/mod/MpConfigSyncCode/**`.重点: `ConfigSyncMessage` 线格式与 BaseLib `ICustomMessage`/`CustomMessageWrapper` 契约(消息 id 跨版本稳定性、`ShouldBroadcast=false` 对主机推送是否正确、无此 mod 的对端是丢弃还是抛异常);`ConfigPropertyScanner` 与 BaseLib `ModConfig.CheckConfigProperties` 谓词逐条对比;**本交接文档 3.3 已给出该对比的答案**;`ConfigSyncApplier` 的中途失败是否留下 restore 无法撤销的部分状态;异常结束路径(放弃/死亡/胜利/返回菜单)是否都能走到 restore;网络线程与静态配置属性的撕裂读写;`InitializeShared` 期间读取的键集合(不止文档点名的两个).
- `ReviewPerfect` - 审 `sts2-perfect/mod/PerfectCode/**`,尤其 `Cards/Perfect.cs`(598 行)与 `Patches/PerfectPoolGatePatch.cs`.重点: 两遍"先偷后阶梯"的 `((L-2)%5)` 槽 x `(L-2)/5` 轮算术在 L=2,3,7,8 及大 L 下是否与 `DEVELOP.md` 第 2 节一致;`CalculatedVars` 的 lambda 是否会被引擎在预期位置调用、canonical 实例是否会抛 `CanonicalModelException`;`MaxUpgradeLevel = int.MaxValue` 在显示层与升级预览层的整数溢出;`TryModifyEnergyCostInCombatLate` 钩子名/签名是否存在、`Ethereal` 关键字在该时点是否可读;`PerfectPoolGatePatch` 后置补丁是否真能原地改返回值(引擎若返回新集合必须替换 `__result`)、卡不存在时移除是否崩溃;`AfterCreated` 预附魔是否会在读档时重复施加.
- `ReviewChaosBridge` - 审 `chaosbridge/src/**`(`Bootstrap.cs`, `Registry.cs`, `PoolOrder.cs`, `PoolAssignment.cs`, `TransformBatchDedup.cs`, `Config.cs`)与 `ChaosBridge.csproj`.重点: 三条硬不变量(静态构造/字段初始化器不得引用 AutoAnthony 类型;所有 AA 反射必须逐个 try/catch;`Harmony.GetPatchInfo` 让位规则)是否每个文件都成立,尤其 `static` 字段初始化器或传递性触碰 AA 类型的 `const`;`PoolOrder` 回写引擎私有池缓存是否写到了 getter 真正读取的字段、等键时排序是否稳定;`TransformBatchDedup` 的前缀/终结器在批次抛异常时是否会泄漏状态、"无放回"集合是否按批次而非全局;`PoolAssignment` 的 `ChaosRunDefinitions.IsRunActive` 门控语义(从 AA 反编译确认)、反复开始/结束是否会留下陈旧池身份;`ChaosBridge.csproj` 的条件编译在 AA 缺席(存根路径)时是否真能编译,列出所有会未定义的符号.

---

## 8. 建议的接手顺序

1. **改名 + 编辑器文本三修** (0.1 / 0.5 A-D).先做 (A) 的 label 修复,因为它是用户最直观的抱怨,且会连带影响 `Min_*`/`Max_*` 的 cfg 键 -- 越早做迁移代价越小.
2. 重建,跑探针 (`dotnet run --project Probe.csproj --no-restore`),部署三处,交给用户实机复测.
3. **反编译 AutoAnthony.dll 研究原版重组机制**(0.2),产出契约文档.
4. 按契约设计并实现新的 `东尼算法-遗物`,含"悔恨"式额外池(0.3).
5. **回填 F05** - 数据集已在 7.1 全部验证完毕,直接按它重写 `VanillaRelicMapping.cs` 即可,无需重新调研.
6. **验证 7.3 的字典顺序问题**(MP 同步风险),优先用"按 template id 排序"从根上消除.
7. 处理 7.2 的 7 条遗留(尤其 `X_RETAIN_ATTACK_BUFF` 文案与 `OurPointsFor` 的 live 定价).
8. 处理 MpConfigSync 的 3 个缺陷(3.3);四个零产出审查按 7.4 的任务书重派.

**不要在没有用户明确许可的情况下发布到 Steam**(Steam Guard 验证码 + VDF description 剥离策略都还没定).
